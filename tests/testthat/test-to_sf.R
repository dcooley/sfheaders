context("sf")

test_that("sf object created",{

  # m <- matrix(1:8, ncol = 2)
  # sfc <- sfheaders::sfc_linestring( m )
  # sf <- sfheaders:::rcpp_make_sf( sfc, c(1L) )
  # expect_true( inherits( sf, c("sf") ) )
  #
  # sf <- sfheaders:::rcpp_make_sf( sfc, c(1) )
  # expect_true( inherits( sf, c("sf") ) )
  #
  # sf <- sfheaders:::rcpp_make_sf( sfc, c("a") )
  # expect_true( inherits( sf, c("sf") ) )
  #
  # expect_error( sfheaders:::rcpp_make_sf( sfc, c(1,2) ) )
  #
  # m <- cbind(c(1,1,2,2), m)
  # sfc <- sfheaders::sfc_linestring( m, linestring_id = 1)

})
